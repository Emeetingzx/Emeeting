<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ll_root"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@color/transparent"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingTop="@dimen/padding_top" >

    <!-- 再包一层LinearLayout，目的是增加两行之间的间距，fix margin bug -->
    <LinearLayout
        android:id="@+id/ll_row"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/selector_quick_action_item_un_selected"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        android:minHeight="@dimen/min_height_35"
        android:orientation="horizontal"
        android:paddingBottom="@dimen/padding_bottom_5"
        android:paddingLeft="@dimen/padding_left"
        android:paddingRight="@dimen/padding_right"
        android:paddingTop="@dimen/padding_top_5" >

        <ImageView
            android:id="@+id/iv_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/layout_margin_left_5"
            android:gravity="center"
            android:textColor="@color/quick_action_item_text_color_un_selected"
            android:textStyle="bold" />
    </LinearLayout>

</LinearLayout>